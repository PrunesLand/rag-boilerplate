# Core (default Ollama + Chroma path)
langchain==1.3.11
langchain-community==0.4.2

chromadb==1.5.9 # If using Chroma as vector store
trafilatura==2.1.0
cdx_toolkit==0.9.39   # Common Crawl CDX index (CRAWL_SOURCE=commoncrawl)
rank-bm25==0.2.2
sentence-transformers==5.6.0
python-dotenv==1.2.2

# Evaluation harness
ragas==0.4.3
datasets==5.0.0

# UI
streamlit==1.58.0

# Optional providers (uncomment the ones you use)
# langchain-openai==0.2.*        # LLM_PROVIDER/EMBEDDING_PROVIDER=openai
# langchain-anthropic==0.2.*     # LLM_PROVIDER=anthropic
# langchain-postgres==0.0.*      # VECTORSTORE_PROVIDER=pgvector
langchain-ollama==1.1.0
langchain-chroma==1.1.0
